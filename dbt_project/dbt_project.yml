name: 'metricpulse'
version: '1.0.0'
config-version: 2

profile: 'metricpulse'

model-paths: ["models"]
clean-targets:
  - "target"
  - "dbt_packages"

models:
  metricpulse:
    staging:
      +materialized: view
    marts:
      +materialized: table
